#include <stdio.h>

/**
 *main- defines entry point
 *
 *Return: 0 at the end of the function
 *
 */

int main(void)
{
char ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch)
}
for (ch = 'A'; ch <= 'Z'; ch++)
{
putchar(ch);
}
putchar ('\n');
return (0);
}
