#include "main.h"
/**
 * _isdigit - checks for a digit
 * @c: character being checked
 * Return: 1 if digit and 0 if otherwise
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
		c = 1;
	else
		c = 0;
	return (c);
}
